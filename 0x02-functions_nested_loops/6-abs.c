#include "main.h"

/**
 * abs - Computes the absolure value of an integer.
 * @c: The integer to be computed
 *
 * Return: The absolute valur of the integer.
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
